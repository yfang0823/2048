const testArray = [
    [0, 2, 0, 2],
    [2, 0, 2, 2],
    [2, 2, 0, 2],
    [2, 0, 2, 0],
]

var test = function() {
    saveTable(testArray)
    // var array = loadTable()
    // rightPlus(array)
    // // saveTable(array)
    // // var array = loadTable()
    // leftPlus(array)
    // // saveTable(array)
    // // var array = loadTable()
    // upPlus(array)
    // // downPlus(array)
    // saveTable(array)
}

test()
